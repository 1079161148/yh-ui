node-linker=hoisted
# 自动安装 peerDependencies，与 npm 7+ 保持一致
auto-install-peers=true
# peerDep 版本不精确匹配时不阻断安装（允许兼容版本）
strict-peer-dependencies=false
# workspace 内部包使用符号链接，确保 monorepo 内引用一致
link-workspace-packages=true
